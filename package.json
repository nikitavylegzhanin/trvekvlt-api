{
  "name": "ext",
  "main": "build/index.js",
  "scripts": {
    "build": "rm -rf ./build && tsc",
    "start": "node -r dotenv/config ./build",
    "dev": "npm run build && npm run start",
    "test": "jest"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^1.6.1",
    "@tinkoff/invest-js": "^1.0.0-alpha.3",
    "apollo-server": "^3.6.1",
    "ramda": "^0.28.0",
    "telegraf": "^4.7.0",
    "typeorm": "^0.2.41"
  },
  "devDependencies": {
    "@types/jest": "27.0.3",
    "@types/node": "15.14.2",
    "@types/ramda": "0.28.13",
    "dotenv": "10.0.0",
    "graphql": "16.5.0",
    "jest": "27.3.1",
    "pg": "8.7.3",
    "ts-jest": "27.0.7",
    "typescript": "4.3.5"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "trailingComma": "es5"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "setupFiles": [
      "dotenv/config"
    ],
    "testMatch": [
      "**/?(*.)+(spec|test).[jt]s?(x)"
    ]
  },
  "renovate": {
    "extends": [
      "config:base"
    ],
    "packageRules": [
      {
        "matchUpdateTypes": [
          "minor",
          "patch",
          "pin",
          "digest"
        ],
        "automerge": true
      }
    ]
  }
}
